import { LetterState, type Letter } from "./letter";
import { Word } from "./word";
import { WordList } from "./wordList";
import { Game, } from "./game";

export class ValidWordList {
  //public static validWordList: string[] = WordList;

  public static findValidWords(guesses: Word[], guessIndex: number, validWordList: string[]): string[] {
    for(const word of validWordList) {
      
    }
    return validWords;
  }
}
